# 火把袋 0.6.0 驗證紀錄

日期：2026-09-21。Windows 11，Java 17 / Java 21。

## 建置與遊戲內測試

兩版均完成 `build`；Forge 成品另完成 `reobfJar`，可供一般安裝使用。

| 測試組合 | Forge 1.20.1 / 47.4.10 | NeoForge 1.21.1 / 21.1.251 |
|---|---|---|
| Curios + 精妙背包 | 通過 | 通過 |
| 都不安裝 | 通過 | 通過 |
| 只安裝 Curios | 通過 | 通過 |
| 只安裝精妙背包及 Core | 通過 | 通過 |

每次執行十個 GameTest 定義；未安裝對應模組時，Curios 或精妙背包的專屬檢查會略過。完整連動組合的十項檢查如下：

1. **容量與保存：** 54 格、拒收非火把、裝滿 3456 支後仍能正確存取、複製物品後保留內容、名字、半徑與光源密度設定。
2. **來源順序：** 設定按鈕能把半徑保存為 64 格並把密度切到中；低／中／高間距為 13／9／8 格；Shift 點擊能裝滿最後一格、禁止移動正在開啟的袋子、優先用袋內火把、用完切換玩家背包、退回保留數量。
3. **Curios：** 自動增加一格腰帶欄位、拒收其他物品、穿戴啟用、穿戴欄優先、取下停用。
4. **補光密度：** 5×5 暗地面在一次掃描中只放一支火把，整塊地面取得方塊光照。
5. **近乎瞬間補光：** 兩個彼此相隔 14 格的暗點在相鄰兩個遊戲刻內各放下一支火把（約 0.1 秒）。
6. **精妙背包：** 發現隨身背包、實際扣除一支、失敗退回、移走背包後不能繼續取用。
7. **移動中補光：** 持續正常走路時，可設定半徑的掃描仍能插下火把。
8. **放置被取消：** 真正的方塊放置事件被取消後，世界回復原狀，火把沒有減少。
9. **範圍與支撐：** 16 格預設球形邊界、不能懸空放置、有支撐可放置、不取代水。
10. **火把爆彈：** 驗證 4／8／16 區塊半徑、螺旋無跳格與重複、負座標、普通區塊固定只取 25 根代表柱且最壞不超過 36 根、右鍵消耗、100% 前拒絕第二顆、兩位玩家各自運行、重疊範圍不加倍，以及不會穿入同區塊內的密閉房間。

實測連動版本：

| 模組 | Forge | NeoForge |
|---|---|---|
| Curios | 5.13.0+1.20.1 | 9.5.1+1.21.1 |
| Sophisticated Backpacks | 3.26.1.2132 | 3.26.3.2158 |
| Sophisticated Core | 1.5.1.2335 | 1.5.1.2341 |

## 畫面檢查

兩個 Minecraft 客戶端都已使用真正的遊戲渲染器輸出一張火把袋畫面，並檢查新貼圖、繁體中文、原版大型箱子底圖、6×9 格子排列、原版風格齒輪、設定面板、四個半徑按鈕與低／中／高光源密度按鈕。簡體中文語系檔也已納入成品並通過 JSON 檢查。

圖片放在 `verification/forge-1.20.1/` 和 `verification/neoforge-1.21.1/`。這是開發用的介面渲染檢查；收納的伺服器邏輯由 GameTest 另外驗證。

## 尚未實測的情況

尚未進行真人多人伺服器的長時間遊玩、逐一測試第三方領地保護模組，以及每一種整合包的特殊生怪規則。已驗證平台放置事件取消的處理，但不能據此聲稱相容所有領地模組。

按鍵封包、完整人工滑鼠操作、死亡後物品回收及跨遊戲版本搬移存檔，未列為這次自動測試通過項目。1.20.1 和 1.21.1 的火把袋資料格式不同，不提供跨版本存檔轉換工具。
